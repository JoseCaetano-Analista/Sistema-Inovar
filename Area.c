#include <stdio.h>

	double = a, b, c;
	double = TriRetangulo, AreaCirculo, AreaTrapezio, AreaQuadrado AreaRetangulo, pi = 3.14159;
	
	scanf(" %If %If %If ", &a, &b, &c);
	
	TriRetangulo = a * (base + altura);
	AreaCirculo = c * ( pi );
	AreaTrapezio = a * b (base) c * (altura);
	AreaQuadrado = area * b;
	AreaRetangulo = a * b;
	
	printf(" %If %If %If %If %If\n", TRIANGULO, CIRCULO, TRAPEZIO, QUADRADO, RETANGULO);
